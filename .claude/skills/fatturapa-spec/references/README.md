Vendored FatturaPA artefacts land in `vendor/fatturapa/` via `tools/fetch_assets.py` (`make schemas`) (XSD 1.2.x + xmldsig-core-schema.xsd, official stylesheet, example files). Record the download URL, date and sha256 in `vendor/SOURCES.md`. A field table extracted from the specs (block number, element, cardinality, type, notes) belongs here as `fields.md` once produced by einvoice-domain-engineer.
